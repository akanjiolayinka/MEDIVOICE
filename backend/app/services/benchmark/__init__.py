# STUB — the benchmark harness (WER, symptom-extraction F1, intent
# accuracy, task completion, latency; comparing Sahara against at least two
# other speech models via the SpeechProvider interface) is Phase 12 of the
# master build plan. Nothing calls into this package yet.
